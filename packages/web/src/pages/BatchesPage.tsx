export function BatchesPage() {
  return (
    <div>
      <h1>Batches</h1>
      {/* TODO: batch list table via useQuery(get('/api/batches')) — iter 3 */}
      <p>TODO: batch list (iter 3)</p>
    </div>
  );
}
