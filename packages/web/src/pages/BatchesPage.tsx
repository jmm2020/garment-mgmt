export function BatchesPage() {
  return (
    <div>
      <h1>Batches</h1>
      {/* TODO iter 3: useQuery({ queryKey: ['batches'], queryFn: () => get<BatchSummary[]>('/api/batches') }) */}
      <p>TODO: batch list (iter 3)</p>
    </div>
  );
}
